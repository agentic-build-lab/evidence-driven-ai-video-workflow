# Contact Sheet Placeholder

This package cannot generate a bitmap contact sheet in the current Codex container because Remotion Chrome and ffmpeg are unavailable. Use this placeholder with `preview.html`, `sync_timeline.json`, and `render_report.md` for review.

Future command once render dependencies are available:

```bash
cd apps/remotion_director
npm run still:demo
npm run render:demo
```
