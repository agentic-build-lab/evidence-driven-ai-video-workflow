{
  "name": "evidence-driven-ai-video-workflow",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "postCreateCommand": "python3 -m pip install --break-system-packages --upgrade pip && python3 -m pip install --break-system-packages -r requirements.txt pytest && cd apps/remotion_director && npm ci",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "dbaeumer.vscode-eslint"
      ]
    }
  }
}
